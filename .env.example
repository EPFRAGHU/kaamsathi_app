# Copy this file to .env for local testing (never commit the real .env).
# Leave DATABASE_URL unset locally and it falls back to a local SQLite file.
DATABASE_URL=
SECRET_KEY=change-this-to-a-long-random-string
